import React from 'react'
import './LineBlack.css'

const LineBlack = (props) => {
    return (
        <>
            <div data-aos="fade-up" className='LineBlack'>{props.title}</div>
        </>
    )
}

export default LineBlack